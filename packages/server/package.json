{
  "name": "script-lab-server",
  "version": "1.0.0",
  "scripts": {
    "start": "nodemon index.ts",
    "build": "tsc && cp web.config build/. && cp -r node_modules build/.",
    "deploy": "node scripts/deploy.js",
    "test": "echo 'no tests'"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "dotenv": "^6.0.0",
    "express": "^4.16.2",
    "request": "^2.87.0"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.5.3",
    "nodemon": "^1.18.3",
    "shelljs": "^0.8.2",
    "typescript": "^2.9.2"
  }
}
