import React from 'react';
import styled from 'styled-components';

export const TextBoxClipboardWrapper = styled.div`
  display: flex;
  border: 1px gray solid;
  border-radius: 4px;

  & > :nth-child(1) {
    flex: 1;
  }
`;
