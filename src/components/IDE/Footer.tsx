import * as React from 'react'
import styled from 'styled-components'

import { Bar } from '../'

const FooterWrapper = styled(Bar)`
  background: green;
`

const Footer = props => <FooterWrapper>Footer</FooterWrapper>

export default Footer
